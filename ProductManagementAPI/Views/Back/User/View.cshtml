@model ProductManagementAPI.Models.User
<div class="mb-4">
    <a href="/user" class="bg-blue-500 p-2 text-white w-fit rounded-sm hover:bg-blue-600 focus:bg-blue-600">Retour � la liste</a>
</div>

<h1 class="mb-4 text-2xl font-bold">Visualisation du produit n� @Model.Id</h1>

<div class="gap-8 grid grid-cols-1">
    <div class="gap-2 flex flex-col">
        <label for="Id" class="font-bold">ID</label>
        <input type="text" id="Id" name="Id" readonly value="@Model.Id" class="border-black bg-slate-200 p-2 rounded-sm border" />
    </div>
    <div class="gap-2 flex flex-col">
        <label for="Username" class="font-bold">Username</label>
        <input type="text" id="Username" name="Username" readonly value="@Model.UserName" class="border-black bg-slate-200 p-2 rounded-sm border" />
    </div>
    <div class="gap-2 flex flex-col">
        <label for="Email" class="font-bold">Email</label>
        <input type="text" id="Email" name="Email" readonly value="@Model.Email" class="border-black bg-slate-200 p-2 rounded-sm border" />
    </div>
</div>